name: Publish public packages

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      version:
        description: SemVer package version (for example, 0.1.0)
        required: true
        type: string

permissions:
  contents: read
  id-token: write

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json

      - name: Resolve package version
        shell: bash
        env:
          CAPSULE_DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            version="$CAPSULE_DISPATCH_VERSION"
          else
            version="${GITHUB_REF_NAME#v}"
          fi

          semver='^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-((0|[1-9][0-9]*)|([0-9A-Za-z-]*[A-Za-z-][0-9A-Za-z-]*))(\.((0|[1-9][0-9]*)|([0-9A-Za-z-]*[A-Za-z-][0-9A-Za-z-]*)))*)?$'
          if [[ ! "$version" =~ $semver ]]; then
            echo "Capsule Tiled releases require strict SemVer without build metadata." >&2
            exit 1
          fi

          echo "CAPSULE_PACKAGE_VERSION=$version" >> "$GITHUB_ENV"

      - name: Restore
        run: dotnet restore --locked-mode -p:CapsuleUsePackages=true

      - name: Build
        run: >
          dotnet build Capsule.Tiled.slnx
          --configuration Release
          --no-restore
          -p:CapsuleUsePackages=true
          -p:Version=$CAPSULE_PACKAGE_VERSION

      - name: Test
        run: dotnet test Capsule.Tiled.slnx --configuration Release --no-build -p:CapsuleUsePackages=true

      - name: Pack
        run: >
          dotnet pack Capsule.Tiled.slnx
          --configuration Release
          --no-build
          --output artifacts/packages
          -p:CapsuleUsePackages=true
          -p:PackageVersion=$CAPSULE_PACKAGE_VERSION

      - name: Retain release artifacts
        uses: actions/upload-artifact@v7
        with:
          name: capsule-tiled-${{ env.CAPSULE_PACKAGE_VERSION }}
          path: artifacts/packages/*.nupkg
          if-no-files-found: error
          retention-days: 7

      - name: Require NuGet.org account
        shell: bash
        env:
          NUGET_USER: ${{ vars.NUGET_USER }}
        run: |
          if [[ -z "$NUGET_USER" ]]; then
            echo "Set the repository Actions variable NUGET_USER to the NuGet.org account name." >&2
            exit 1
          fi

      - name: Authenticate to NuGet.org
        id: nuget
        uses: NuGet/login@v1
        with:
          user: ${{ vars.NUGET_USER }}

      - name: Publish to NuGet.org
        shell: bash
        run: |
          for package in artifacts/packages/*.nupkg; do
            dotnet nuget push "$package" \
              --source https://api.nuget.org/v3/index.json \
              --api-key "${{ steps.nuget.outputs.NUGET_API_KEY }}" \
              --skip-duplicate
          done
